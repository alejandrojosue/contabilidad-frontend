---
import Layout from "@layouts/Layout.astro";
import MenuCardOption from "@common/MenuCardOption";
import config from "@constants/config.json";

const options = config.pages.company.options.map((option) => {
    return {...option, link: `${config.pages.company.menu}${option.link}`};
});
---

<Layout>
    <!-- @ts-ignore -->
    <MenuCardOption data={options} title="Menú empresa" client:only/>
</Layout>

